<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title }}</title>
  <style>
    /* Embed your local Inter fonts (exact filenames) */
    @font-face {
      font-family: "Inter";
      src: url("assets/fonts/Inter_18pt-Regular.ttf") format("truetype");
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: "Inter";
      src: url("assets/fonts/Inter_18pt-Bold.ttf") format("truetype");
      font-weight: 700;
      font-style: normal;
      font-display: swap;
    }

    :root {
      --ink: #0b1220;
      --muted: #6b7280;
      --brand: #1f6feb;
      --link: #2563eb;
      --line: #e5e7eb;
    }

    @page {
      size: A4;
      margin: 20mm 15mm 20mm 15mm;

      @bottom-right {
        content: "Generated: {{ generated_at }} \2014 Page " counter(page) " of " counter(pages);
        font-size: 10px;
        color: var(--muted);
      }
    }

    * {
      box-sizing: border-box
    }

    body {
      font-family: "Inter", Arial, sans-serif;
      color: var(--ink);
      margin: 0;
      line-height: 1.55;
      font-weight: 400
    }

    .page {
      padding: 40px 56px
    }

    h1 {
      font-size: 28px;
      margin: 0 0 8px;
      font-weight: 700
    }

    h2 {
      font-size: 18px;
      margin: 24px 0 8px;
      border-bottom: 1px solid var(--line);
      padding-bottom: 6px;
      font-weight: 700
    }

    p {
      line-height: 1.55
    }

    .badge {
      display: inline-block;
      padding: 2px 8px;
      border: 1px solid var(--brand);
      color: var(--brand);
      border-radius: 999px;
      font-size: 12px;
      font-weight: 700
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 8px 0 16px;
      font-size: 12.5px
    }

    th,
    td {
      border: 1px solid var(--line);
      padding: 6px 8px;
      text-align: left;
      vertical-align: top
    }

    th {
      background: #f8fafc;
      font-weight: 700
    }

    ul {
      margin: 6px 0 12px 18px
    }

    .footer {
      margin-top: 24px;
      font-size: 12px;
      color: var(--muted)
    }

    a,
    a:visited {
      color: var(--link);
      text-decoration: underline;
      word-break: break-all;
    }
  </style>
</head>

<body>
  <div class="page">
    <h1>{{ title }}</h1>
    <p>
      <span class="badge">{{ frequency | capitalize }}</span>
      &nbsp; <strong>Media Team:</strong> {{ media_team }}
      &nbsp; | &nbsp; <strong>Owner:</strong> {{ owner.name }} ({{ owner.email }})
    </p>
    {{ content | safe }}
    <div class="footer">Generated by Documentation GPT • Version {{ version }}</div>
  </div>
</body>

</html>